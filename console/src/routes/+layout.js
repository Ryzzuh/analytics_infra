// A static bundle with client-side rendering: everything on the page is live state from the
// control plane, so there is nothing meaningful to prerender.
export const prerender = false;
export const ssr = false;
